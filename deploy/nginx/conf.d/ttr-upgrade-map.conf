# Maps the Upgrade header so proxied WebSocket/SSE connections keep alive,
# while normal requests send "Connection: close". Required by ttr-proxy.conf.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
