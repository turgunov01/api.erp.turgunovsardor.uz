apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ttr-one.configMapName" . }}
  labels:
    {{- include "ttr-one.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
